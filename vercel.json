{
  "version": 2,
  "buildCommand": "cd frontned && npm install && npm run build",
  "outputDirectory": "frontned/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
